version: '3.8'
services:
#  mysql:
#    image: mysql:8.0
#    container_name: local-mysql
#    environment:
#      MYSQL_ROOT_PASSWORD: password
#      MYSQL_DATABASE: test
##      MYSQL_USER: test_user
##      MYSQL_PASSWORD: password
#    ports:
#      - "3306:3306"
##    volumes:
##      - ./docker-db-init.sql:/docker-entrypoint-initdb.d/init.sql  # init.sql 파일을 컨테이너에 마운트
#    #   - mysql_data:/var/lib/mysql

  postgres:
    image: postgres:15
    container_name: local-postgresql
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password
      POSTGRES_DB: test
    ports:
      - "5432:5432"
    volumes:
      - ./docker-db-init.sql:/docker-entrypoint-initdb.d/init.sql  # init.sql 파일을 컨테이너에 마운트
    #   - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7.0
    container_name: local-redis
    ports:
      - "6379:6379"

# volumes:
#   mysql_data:
#   postgres_data:
